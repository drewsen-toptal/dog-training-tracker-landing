// Release.xcconfig
// Release configuration for iOS projects - minimal overrides only
// Generated by XcodeBuildMCP

#include "Shared.xcconfig"

// ==========================================
// Release-specific Settings
// ==========================================
// Exclude StoreKit configuration file from release builds
// This prevents the "Purchases are only available when testing from Xcode" error
EXCLUDED_SOURCE_FILE_NAMES = $(inherited) *.storekit